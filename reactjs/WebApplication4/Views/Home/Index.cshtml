@{
    Layout = null;
}

<!DOCTYPE html>
<script src="~/Scripts/react/react.js"></script>
<script src="~/Scripts/react/react-dom.js"></script>
<script src="~/Scripts/react/browser.min.js"></script>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>bbbbbbbb</p>
    <div id="example"></div>

    <div id="example2"></div>

    <select id="example3">
        <option value="">全部</option>
    </select>

    <div id="example4"></div>
    <script type="text/babel">
        @*example****************************************开始*@
        ReactDOM.render(
        <h1>Hello, world!</h1>,
        document.getElementById('example')
        );
        @*example****************************************end*@

        @*example2****************************************开始*@

        var names = ['Alice', 'Emily', 'Kate'];

        ReactDOM.render(
        <div>
            @*第一个循环****************************************开始*@
            {
            names.map(function (name, index) {
            return <div key={index}>Hello, {name},你是第 {index}个人!</div>
            })
            }
            @*第一个循环****************************************end*@

            @*第2个循环****************************************begin*@
            {
            names.map(function (name) {
            return <div>Hello, {name}!</div>
            })
            }
            @*第2个循环****************************************end*@
        </div>,
        document.getElementById('example2')
        );
        @*example2****************************************end*@
        @*example3****************************************开始*@
        var searchs = ['第一组', '第二组', '第三组'];
        ReactDOM.render(
        <div>
            {
            searchs.map(function (name, index) {
            return <option key={index}>Hello, {name},你是第 {index}个人!</option>
            })
            }
        </div>,
        document.getElementById('example3'));
        @*example3****************************************end*@

        @*example4****************************************开始*@
        var HelloMessage = React.createClass({
        render: function() {
        return
        <h1>Hello {this.props.name}</h1>;
        }
        });

        ReactDOM.render(
        <hellomessage name="John" />,
        document.getElementById('example4')
        );

        @*example4****************************************end*@
    </script>
</body>
</html>
